using Goap;
using UnityEngine;

public class bad_manager : MonoBehaviour
{


    world_states current_worldstate;
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Awake()
    {
      
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
